import Leaderboard from '../user/Leaderboard';

// Reuse the user leaderboard component for the admin panel
const AdminLeaderboard = () => {
  return (
    <div>
      <Leaderboard />
    </div>
  );
};

export default AdminLeaderboard;
